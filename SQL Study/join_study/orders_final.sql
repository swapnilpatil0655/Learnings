CREATE TABLE `Orders` (
	`OrderID` DECIMAL(38, 0) NOT NULL, 
	`CustomerID` DECIMAL(38, 0) NOT NULL, 
	`EmployeeID` DECIMAL(38, 0) NOT NULL, 
	`OrderDate` DATE NOT NULL, 
	`ShipperID` DECIMAL(38, 0) NOT NULL
);
